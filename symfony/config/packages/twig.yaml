twig:
    paths:
        #base.html.twig
        '%kernel.project_dir%/src/Infrastructure/Shared/Templates': ~

        '%kernel.project_dir%/src/Infrastructure/Users/Http/Resources/views': 'users'
        '%kernel.project_dir%/src/Infrastructure/BetDataProvider/Http/Resources/views': 'bets'
    globals:
        app_version: '%env(APP_VERSION)%'
        


# twig:
#     default_path: '%kernel.project_dir%/templates'

# when@test:
#     twig:
#         strict_variables: true
